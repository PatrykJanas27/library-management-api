spring:
  datasource:
    url: jdbc:mysql://localhost:3307/library
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Mysqlqwerty3241
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher